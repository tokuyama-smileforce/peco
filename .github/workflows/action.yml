name: action

on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-18.04
  steps:
  - name: Checkout
  - uses: actions/checkout@main
  - name: Run
  - uses: nanasess/setup-php@main
    with:
      php-version: '5.6'
  - run: php index.php